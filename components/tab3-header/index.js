import { useState } from 'react';

import {
	StyledMenuTitle,
} from './styles';

function Tab3Header() {
	const [count, setCount] = useState(0);

	return (
		<StyledMenuTitle>
			<h1>123123</h1>
		</StyledMenuTitle>
	);
}

export default Tab3Header;
